<h1>All players</h1>

<% @players.each do |player| %>
  <ul>
    <li>Name: <%= link_to player.name, player_path(player.id) %></li>
    <li>Country: <%= player.country %>
    <li>Position: <%= player.position %> </li>
    <li>Number: <%= player.number %> </li> -->
  </ul>
<% end %>
<% end %>
